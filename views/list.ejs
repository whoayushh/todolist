<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To do List</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>
<style>   
body{
    background-color: #eaf6d4;
}
.wrapper{
    display: grid;
    place-items:  center;
}
.date-heading{
    background-color: rgb(1, 121, 121);
    padding: 5px 5px 5px 5px;
    border-radius: 5px 5px 5px 5px;
}
.items{
    display: flex;
    /* background-color: rgb(152, 247, 201); */
}

.list-items{
    margin-left: -10rem;
    gap:2rem;
    /* line-height: 2rem; */
    
    
}
#btn{
    color:blue;
}
.delete-button {
    visibility: hidden;
    margin-left: 10px;
    padding: 5px 10px;
    background-color: none;
    /* color: #fff; */
    cursor: pointer;
    
}
</style>
<body>
    <div class="wrapper">
        <h1 class = "date-heading"><%= kindOfDay %></h1>

    <div class="list-items">
        <ul type="none">
            
            <% for(let i=0; i<newListItems.length; i++){ %>
                <div class="items">
                    <input type="checkbox" onchange="toggleStrikeThrough(this)" >
                    <li><%= newListItems[i] %></li>
                    <button class="delete-button" onclick="deleteItem(this)"><i class="fa-solid fa-trash"></i></button>
                </div>
    
            <% } %>
            
        </ul>
    </div>

    <form action="/" method="post">
        
        <input type="text" name="newItem" autocomplete="off">
        <button type="submit" id="btn">Add</button>
    </form>
    </div>


   
    <script>
        function toggleStrikeThrough(checkbox) {
            var textSpan = checkbox.nextElementSibling;
            var deleteButton = textSpan.nextElementSibling;

            textSpan.style.textDecoration = checkbox.checked ? 'line-through' : 'none';
            deleteButton.style.visibility = checkbox.checked ? 'visible' : 'hidden';
        }
        function deleteItem(button) {
            var listItem = button.closest('li');
            listItem.remove();
        }
    </script>
</body>
</html>
